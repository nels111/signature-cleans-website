// ============================================
// SIGNATURE CLEANS - Site Config
// Edit these values before deploying.
// ============================================

window.SIGNATURE_CONFIG = {
  // If you use Formspree, set to: https://formspree.io/f/XXXXXXX
  formEndpoint: "",

  // Optional: an endpoint you control that pushes into Zoho CRM.
  // (Recommended approach: a small serverless function that validates + forwards.)
  zohoWebhook: "",

  // Optional: your GA4 Measurement ID, e.g. "G-XXXXXXXXXX"
  gaMeasurementId: "",

  // Optional: Google reCAPTCHA v3 site key (if you add it)
  recaptchaSiteKey: ""
};
